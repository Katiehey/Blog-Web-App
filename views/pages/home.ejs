<div class="container">
    <div class="hero-section">
        <h1 class="page-title">Latest Thoughts</h1>
        <p class="page-subtitle">Ideas, stories, and insights worth sharing</p>
    </div>
    
    <div class="post-list">
        <% if (posts.length === 0) { %>
            <div class="empty-state">
                <div class="empty-state-icon">📝</div>
                <h3>No posts yet</h3>
                <p>Be the first to share your thoughts!</p>
                <a href="/compose" class="primary-btn" style="margin-top: 1rem;">
                    ✏️ Create First Post
                </a>
            </div>
        <% } else { %>
            <% posts.forEach((post, index) => { %>
                <a href="/posts/<%= post.id %>" class="post-item">
                    <h2><%= post.title %></h2>
                    <p class="post-snippet"><%= post.content.substring(0, 180) + '...' %></p>
                    <div class="post-meta">
                        <span class="post-date">📅 Just now</span>
                        <span class="read-time">⏱️ 2 min read</span>
                    </div>
                    <div class="read-more">Read more →</div>
                </a>
            <% }) %>
        <% } %>
    </div>
</div>